# 普通用户与系统管理员的身份操作差异及前端体现
基于《基于混合索引的单细胞高维向量数据近似最近邻(ANN)智能检索系统》的需求分析与UML设计，普通用户与系统管理员在**身份定位、权限范围、核心操作**上存在明确分层，前端通过**入口隔离、权限拦截、界面差异化、操作限制**四种方式实现身份区分与权限控制。

## 一、核心身份与定位差异
| 角色 | 身份定位 | 核心目标 | 系统权限层级 |
|------|----------|----------|--------------|
| 普通注册用户 | 系统主要使用者（生物医学研究员、生物信息学分析师） | 完成单细胞数据上传、索引构建、相似性检索与结果分析 | 基础功能权限，仅可操作自有资源 |
| 系统管理员 | 系统维护者与运营者 | 保障系统稳定运行、管理用户与资源、审计系统行为 | 全量权限，继承普通用户所有功能+全局管理权限 |

## 二、核心操作权限差异
按系统功能模块划分，两者的操作边界如下：

### 1. 用户认证与账户管理
| 操作项 | 普通用户 | 系统管理员 |
|--------|----------|------------|
| 注册/登录 | 支持手机号/邮箱注册，账号密码登录；异常登录需二次验证 | 仅可通过预设账号登录，**强制双因素认证（短信+密码）**；支持普通用户登录的所有流程 |
| 个人信息管理 | 修改自身密码、邮箱、基本资料；找回密码 | 可修改自身信息，同时**查看/编辑所有用户的基本信息** |
| 权限管理 | 无权限，仅能使用系统分配的默认权限 | **分配/调整用户角色权限**（普通用户/科研用户/分析师）；冻结/解冻/注销用户账号 |
| 账号安全 | 查看自身登录记录；开启/关闭异地登录提醒 | 查看所有用户的登录日志；强制下线异常账号；设置全局密码复杂度规则 |

### 2. 数据管理模块
| 操作项 | 普通用户 | 系统管理员 |
|--------|----------|------------|
| 数据上传 | 仅可上传自有单细胞数据集（h5ad/loom/Seurat等格式） | 可上传全局公共数据集；可导入用户提交的离线数据 |
| 数据查看 | 仅可见**自己上传的数据集**及授权共享的公共数据集 | 可见系统内所有用户的数据集；查看数据集的上传者、大小、状态等全量信息 |
| 数据操作 | 对自有数据执行预处理、元数据编辑、删除、导出 | 可对任意数据集执行删除、备份、转移；**批量清理过期/违规数据** |
| 数据权限 | 设置自有数据集的隐私级别（私有/公开/共享） | 调整任意数据集的全局访问权限；下架违规公开数据集 |

### 3. 索引管理模块
| 操作项 | 普通用户 | 系统管理员 |
|--------|----------|------------|
| 索引构建 | 为自有数据集构建HNSW/IVF/PQ/混合索引；选择索引参数 | 为任意数据集构建索引；**强制重建异常索引** |
| 索引管理 | 查看自有索引的状态、大小、构建时间；加载/删除自有索引 | 查看系统内所有索引的全局统计；**批量删除过期索引（30天未使用）**；监控索引内存占用 |
| 索引配置 | 使用系统推荐的默认参数；手动调整基础参数 | 可修改全局索引默认参数；配置智能决策树的权重规则 |
| 索引持久化 | 下载自有索引文件到本地 | 备份全局索引文件；恢复损坏的索引数据 |

### 4. 检索与可视化模块
| 操作项 | 普通用户 | 系统管理员 |
|--------|----------|------------|
| 检索执行 | 对自有/公共数据集执行精确/近似检索、元数据过滤检索、批量检索 | 可执行所有普通用户的检索操作；可模拟任意用户的检索请求进行测试 |
| 结果查看 | 查看自身检索的Top-K结果、相似度热力图、UMAP降维图 | 可查看任意用户的检索历史与结果 |
| 性能监控 | 查看自身查询的响应时间、召回率等指标 | 查看**全局系统性能**：并发请求数、平均查询延迟、错误率、服务器负载 |
| 结果导出 | 导出自身检索结果为h5ad/CSV/JSON等格式 | 可导出任意检索结果；导出全局性能统计报表 |

### 5. 系统管理与审计
| 操作项 | 普通用户 | 系统管理员 |
|--------|----------|------------|
| 系统配置 | 无权限 | 修改系统全局配置：文件上传大小限制、索引保留期限、并发数阈值 |
| 日志审计 | 仅查看自身的操作日志（上传/检索/构建索引） | **查看/导出全量系统审计日志**：所有用户的操作记录、异常访问日志、系统错误日志 |
| 数据运维 | 无权限 | 执行系统数据备份与恢复；清理系统临时文件与冗余数据 |
| 系统监控 | 无权限 | 实时监控服务器CPU、内存、磁盘使用率；查看服务运行状态；重启异常服务 |

## 三、前端层面的身份差异体现
前端通过**分层设计**实现身份隔离，确保普通用户无法访问管理员功能，同时为管理员提供高效的管理界面，具体体现如下：

### 1. 导航栏与功能入口隔离
- **普通用户导航栏**（核心功能导向）：
  首页 → 数据管理 → 索引构建 → 检索中心 → 可视化分析 → 个人中心
- **管理员导航栏**（基础功能+管理功能）：
  首页 → 数据管理 → 索引构建 → 检索中心 → 可视化分析 → **系统管理**（下拉菜单：用户管理、索引监控、日志审计、系统配置） → **资源看板** → 个人中心
- 实现方式：前端根据登录接口返回的`role_type`字段（`user`/`admin`）动态渲染导航项，管理员专属入口对普通用户完全隐藏。

### 2. 页面访问权限控制
- 所有管理员专属页面（如`/admin/users`、`/admin/logs`）添加**路由守卫**：
  - 普通用户直接访问管理员路由时，自动跳转至403权限拒绝页面，并提示"您没有访问该页面的权限"
  - 未登录用户访问任何功能页面时，跳转至登录页
- 管理员可访问普通用户的所有页面，且在页面右上角显示"管理员模式"标识，便于区分身份。

### 3. 操作按钮与交互权限控制
- **数据管理页面**：
  - 普通用户：仅显示"上传数据"、"编辑我的数据"、"删除我的数据"按钮；数据集列表仅展示自有数据
  - 管理员：额外显示"上传公共数据"、"强制删除"、"修改权限"、"查看所有者"按钮；数据集列表展示所有用户数据，并增加"上传者"、"隐私级别"列
- **索引管理页面**：
  - 普通用户：仅可对自有索引执行"重建"、"下载"、"删除"操作
  - 管理员：可对任意索引执行"强制删除"、"批量清理过期索引"操作；显示索引的内存占用、服务器节点等运维信息
- **用户管理页面**：仅管理员可见，包含用户列表、角色分配、账号状态管理等功能。

### 4. 数据可见性与界面布局差异
- **数据过滤**：前端请求后端数据时，自动携带当前用户的`user_id`，后端仅返回该用户有权限查看的数据；管理员请求时返回全量数据
- **性能监控面板**：
  - 普通用户：仅显示"我的查询统计"（今日查询次数、平均响应时间）
  - 管理员：显示"全局系统监控"（CPU使用率、内存占用、并发请求数、索引总数、活跃用户数）
- **日志页面**：
  - 普通用户：仅展示自身操作日志，支持按时间筛选
  - 管理员：支持按用户ID、操作类型、IP地址等多维度筛选日志；支持导出Excel格式的审计报表

### 5. 安全验证与提示差异
- 管理员登录时，前端额外显示**双因素认证输入框**（短信验证码），验证通过后方可进入系统
- 管理员执行高危操作（如删除用户、批量清理数据）时，前端弹出**二次确认弹窗**，要求输入管理员密码进行验证
- 普通用户执行删除自有数据等操作时，仅需简单确认，无需密码验证

### 6. 异常处理与引导差异
- 普通用户遇到权限不足时，提示"请联系系统管理员申请权限"
- 管理员遇到系统异常时，额外显示"查看错误日志"按钮，可直接跳转至日志审计页面查看详细报错信息

需要我基于这些差异，生成一份**前端路由配置和权限守卫的示例代码**（React+React Router）吗？