# 多数据集联合检索说明

## 1. 功能目标

本项目已经支持多个单细胞数据集联合建立一个 FAISS 向量索引，从而实现跨数据集细胞相似性检索。

典型用途：

- 将 `data/liver.h5ad` 和 `data/1.h5ad` 合并到同一个索引中。
- 输入一个细胞 ID，在多个数据集范围内查找最相似的细胞。
- 使用 `dataset_name` 筛选指定数据集，避免重复 cell_id 产生歧义。
- 在同一查询页面查看跨数据集 Top-K 结果、UMAP 可视化、统计图和 ANN/Exact 评估结果。

## 2. 使用方式

在索引构建页面的 Dataset 输入框中，用分号分隔多个数据集路径：

```text
data/liver.h5ad; data/1.h5ad
```

也支持换行或英文逗号分隔，但推荐使用分号，最清晰：

```text
data/liver.h5ad; data/1.h5ad
```

然后点击：

```text
Check Data -> Build Index
```

构建成功后，系统会生成一个联合 collection，名称通常类似：

```text
joint_liver_1_20260708_xxxxxx
```

## 3. 代码实现位置

主要涉及以下文件：

```text
services/data_loader.py
services/faiss_engine.py
services/vector_index.py
faiss_service.py
app.py
static/app.js
templates/index.html
```

核心函数：

- `_parse_data_paths()`
- `load_joint_cell_vectors()`
- `inspect_cell_dataset()`
- `load_dataset_visualization_preview()`
- `load_dataset_analytics()`
- `_resolve_original_cell_id_offset()`
- `_resolve_service_data_path()`

## 4. 联合索引构建流程

### 4.1 路径解析

`services/data_loader.py` 中的 `_parse_data_paths()` 会把输入路径拆成多个数据集：

```text
data/liver.h5ad; data/1.h5ad
```

解析为：

```text
data/liver.h5ad
data/1.h5ad
```

### 4.2 单数据集加载

每个数据集仍然按原有方式加载：

- `.h5ad` 通过 AnnData 读取。
- `.csv` 通过 Pandas 读取。
- 提取细胞 ID、向量、metadata、UMAP 坐标和质量统计字段。

### 4.3 向量维度检查

联合索引要求所有数据集的向量维度一致。

例如两个数据集都是：

```text
vector_dim = 30
```

则可以联合建索引。

如果维度不同，系统会拒绝构建并返回类似错误：

```text
joint index requires matching vector dimensions
```

原因是 FAISS 同一个索引只能接收相同维度的向量。

### 4.4 向量合并

多个数据集的向量会按行拼接：

```text
liver vectors
1.h5ad vectors
        |
        v
joint vectors
```

最终 FAISS 索引面对的是一个大的向量矩阵。

### 4.5 cell_id 加前缀

跨数据集时，不同数据集可能存在相同的原始 cell_id。为避免冲突，系统会给 cell_id 加数据集前缀：

```text
liver::AAACCTGAGCAGGTCA-1_2
1::AAACCTGAGCAGGTCA-1_2
```

其中：

- `liver` 来自 `liver.h5ad` 文件名。
- `1` 来自 `1.h5ad` 文件名。
- `::` 后面是原始细胞 ID。

metadata 中也会保留：

```text
dataset_id
dataset_name
source_path
original_cell_id
```

## 5. 跨数据集查询逻辑

联合索引构建成功后，查询逻辑不需要单独切换模式。

输入：

```text
liver::AAACCTGAGCAGGTCA-1_2
```

系统会直接按完整 cell_id 查询。

如果输入原始 cell_id：

```text
AAACCTGAGCAGGTCA-1_2
```

系统会尝试通过 metadata 中的 `original_cell_id` 查找。

如果该原始 ID 只出现在一个数据集中，可以正常查询。

如果该原始 ID 在多个数据集中重复，系统会提示：

```text
细胞 ID 在多个数据集中重复，请在 Dataset 筛选中选择一个数据集，或使用带前缀的 cell_id。
```

这时有两种处理方式：

1. 在查询筛选中选择 `Dataset = liver` 或 `Dataset = 1`。
2. 直接输入带前缀的完整 cell_id。

## 6. Dataset 筛选

联合索引会在前端筛选区增加 `Dataset` 选项。

筛选字段来自 metadata：

```text
dataset_name
```

它的作用：

- 限定查询结果只来自某一个数据集。
- 解决重复 cell_id 的歧义。
- 对比同一个查询细胞在不同数据集中的邻居分布。

例如：

```text
Dataset = liver
Cell Type = T cell
```

表示只在 liver 数据集中的 T cell 范围内检索。

## 7. 可视化处理

多数据集联合后，UMAP 坐标会进行横向偏移，避免不同数据集的原始 UMAP 坐标完全重叠。

实现逻辑：

```text
第 1 个数据集使用原始 UMAP
第 2 个数据集在 X 方向偏移
第 3 个数据集继续偏移
```

因此联合可视化的目的主要是：

- 区分不同数据集的细胞分布。
- 支持跨数据集检索结果高亮。
- 支持按数据集、细胞类型、疾病、组织等 metadata 筛选。

注意：不同数据集的 UMAP 坐标通常不是同一个低维空间直接训练得到的，因此联合 UMAP 更适合作为检索结果定位辅助，不应过度解读为严格的全局生物空间。

## 8. 统计图处理

联合索引下，统计分析会合并多个数据集的概览信息。

当前支持：

- 总细胞数合并。
- 基因数汇总。
- 细胞类型分布合并。
- 样本分布合并，并给样本名增加数据集前缀。
- 质量统计直方图合并。
- 箱线图数据合并。

样本名会变成类似：

```text
liver:sampleA
1:sampleB
```

这样可以避免不同数据集里 sample 名称相同导致混淆。

## 9. ANN/Exact 评估

联合索引仍然支持 ANN 与 Exact 对比。

打开：

```text
Enable ANN vs Exact evaluation
```

查询后可以看到：

- ANN 查询耗时。
- Exact 查询耗时。
- Precision@K。
- Recall@K。
- 命中重叠。
- 索引文件占用。
- FAISS 服务内存。

跨数据集时，Precision@K 和 Recall@K 的计算方式不变：

```text
Precision@K = |ANN Top-K ∩ Exact Top-K| / |ANN Top-K|
Recall@K = |ANN Top-K ∩ Exact Top-K| / |Exact Top-K|
```

## 10. 资源占用

联合索引的索引文件通常会比单数据集更大，因为它包含多个数据集的：

- 向量矩阵。
- FAISS 索引文件。
- metadata。
- cell_ids。
- UMAP 坐标。
- manifest 信息。

界面中的 `索引文件占用` 来自 collection 目录下所有文件大小之和。

界面中的 `FAISS服务内存` 来自 8000 FAISS 服务进程的 RSS。

## 11. Docker 与路径要求

如果使用 Docker 中的 FAISS 服务，数据集必须放在项目目录内，默认推荐：

```text
data/liver.h5ad
data/1.h5ad
```

不要输入 Windows 绝对路径，例如：

```text
D:\softwareEgeneering\SEwork\data\liver.h5ad
```

推荐输入工作区相对路径：

```text
data/liver.h5ad; data/1.h5ad
```

如果报错：

```text
data file not found inside container
```

优先检查：

- 文件是否确实在项目 `data` 目录下。
- 路径是否使用了分号分隔。
- Docker Compose 是否把项目目录挂载到 FAISS 服务。
- FAISS 容器是否重启加载了新版代码。

重启 FAISS 服务：

```powershell
docker compose up -d --build --force-recreate faiss
```

## 12. 是否需要重新构建索引

以下情况需要重新构建索引：

- 从单数据集改成多数据集联合索引。
- 增加或删除参与联合检索的数据集。
- 数据集文件内容改变。
- 向量来源、PCA 或 embedding 发生变化。
- 修改了索引类型或主要索引参数。

以下情况一般不需要重新构建索引：

- 只修改前端样式。
- 只修改查询 Top-K。
- 只修改筛选条件。
- 只打开或关闭 ANN/Exact 评估。

## 13. 支持多少个数据集

当前代码没有写死数据集数量上限。

理论上可以联合多个数据集，只要满足：

- 所有数据集向量维度一致。
- 内存足够加载所有 `.h5ad` 和向量矩阵。
- 磁盘足够保存联合 collection。
- FAISS 服务进程有足够内存构建索引。

实际建议：

- 课程项目或本机演示：2 到 5 个数据集比较稳。
- 数据量较大时，先从 2 个数据集测试。
- 如果细胞数超过几十万，需要重点关注构建耗时、FAISS 内存和索引文件大小。

## 14. 常见问题

### 14.1 为什么输入原始 cell_id 查不到？

联合索引中真实 cell_id 已加前缀。

建议使用：

```text
liver::AAACCTGAGCAGGTCA-1_2
```

或者在 Dataset 筛选中选择具体数据集后，再输入原始 cell_id。

### 14.2 为什么两个数据集不能联合构建？

最常见原因是向量维度不同。

FAISS 一个索引只能存储同维度向量。如果一个数据集是 30 维，另一个是 50 维，就不能直接联合。

### 14.3 为什么联合后图表看起来不如单数据集完整？

不同数据集的 metadata 字段可能不一致。某个图表依赖的字段如果在部分数据集中缺失，就会出现统计减少或空图。

当前代码会尽量合并可用字段，但不能凭空补出不存在的 metadata。

### 14.4 为什么跨数据集结果要谨慎解释？

只有当多个数据集的向量表示处于可比较空间时，跨数据集相似性才更可靠。

如果两个数据集使用不同预处理、不同 embedding 或不同批次校正方式，向量距离可能包含技术差异，而不完全是生物相似性。

## 15. 总结

多数据集联合检索的核心流程是：

```text
多个单细胞数据集
  -> 分别读取向量和 metadata
  -> 检查向量维度一致
  -> cell_id 加数据集前缀
  -> 合并向量矩阵和 metadata
  -> 建立一个联合 FAISS 索引
  -> 支持跨数据集 Top-K 检索
```

它让项目从“单数据集内相似细胞搜索”扩展到“跨数据集细胞搜索”，适合用于比较不同来源、不同样本或不同条件下的细胞相似性。
