# 前端依赖接口清单

## 文档目的

本清单用于约定前端页面与后端服务之间的接口边界，便于前端独立开发、后续联调和多人协作时减少冲突。

适用文件：

- `templates/index.html`
- `static/app.js`
- `static/styles.css`
- `README.md`

## 前端依赖原则

- 前端只通过 HTTP API 与后端交互，不直接依赖 `services/` 内部实现。
- 若后端实现调整，优先保持接口路径、请求字段和响应字段稳定。
- 若字段必须变更，应先同步本清单，再统一调整前端适配代码。
- 失败响应建议统一返回：`{ "error": "错误信息" }`。

## 页面与接口映射

| 页面功能 | 接口 | 方法 | 前端入口 |
| --- | --- | --- | --- |
| 系统状态检查 | `/api/health` | GET | 页面初始化、状态刷新 |
| 数据集检查 | `/api/dataset/inspect` | POST | “检查数据”按钮 |
| 构建索引 | `/api/index/build` | POST | “构建索引”按钮 |
| 按细胞 ID 查询 | `/api/search/by-id` | POST | “细胞 ID”查询按钮 |
| 按向量查询 | `/api/search/by-vector` | POST | “向量”查询按钮 |
| 用户注册 | `/api/auth/register` | POST | “注册”按钮 |
| 用户登录 | `/api/auth/login` | POST | “登录”按钮 |
| 获取当前用户 | `/api/auth/me` | GET | 页面初始化后的登录态恢复 |
| 管理员查看用户 | `/api/admin/users` | GET | “用户管理”按钮 |
| 管理员新增用户 | `/api/admin/users` | POST | 用户管理面板 |
| 管理员修改用户 | `/api/admin/users/<user_id>` | PATCH | 预留给后续管理功能 |
| 管理员删除用户 | `/api/admin/users/<user_id>` | DELETE | 用户管理面板 |

## 接口明细

### 1. 系统状态检查

- 路径：`GET /api/health`
- 用途：获取系统是否已构建索引，以及当前数据集摘要。

返回示例：

```json
{
  "status": "ok",
  "indexed": true,
  "dataset": {
    "cell_count": 1000,
    "gene_count": 2000,
    "vector_dim": 30
  }
}
```

前端使用字段：

- `status`
- `indexed`
- `dataset`

### 2. 数据集检查

- 路径：`POST /api/dataset/inspect`
- 用途：在构建索引前检查数据文件是否可用，并展示数据摘要。

请求示例：

```json
{
  "data_path": "liver.h5ad"
}
```

返回字段约定：

- `source_path`
- `format`
- `cell_count`
- `gene_count`
- `vector_dim`
- `embedding_key`

### 3. 构建索引

- 路径：`POST /api/index/build`
- 用途：加载数据并构建 Qdrant/HNSW 索引。

请求示例：

```json
{
  "data_path": "liver.h5ad"
}
```

返回示例：

```json
{
  "message": "index built",
  "collection": "cell_vectors",
  "cell_count": 1000,
  "gene_count": 2000,
  "vector_dim": 30,
  "embedding_key": "X_pca",
  "build_time_ms": 1234.56
}
```

前端使用字段：

- `collection`
- `cell_count`
- `gene_count`
- `vector_dim`
- `embedding_key`
- `build_time_ms`

### 4. 按细胞 ID 查询

- 路径：`POST /api/search/by-id`
- 用途：以已有细胞为查询中心，返回 Top-K 相似细胞。

请求示例：

```json
{
  "cell_id": "AAACCTGAGCAGGTCA-1_2",
  "top_k": 10,
  "filters": {
    "cell_type": "T cell",
    "disease": "healthy",
    "AgeGroup": "adult"
  }
}
```

返回字段约定：

- `query`
- `query_time_ms`
- `results`

单条结果建议包含：

- `cell_id`
- `distance`
- `score`
- `metadata.cell_type`
- `metadata.disease`
- `metadata.AgeGroup`
- `metadata.sex`

### 5. 按向量查询

- 路径：`POST /api/search/by-vector`
- 用途：输入一个向量并返回 Top-K 相似细胞。

请求示例：

```json
{
  "vector": [0.12, 0.34, 0.56],
  "top_k": 10,
  "filters": {}
}
```

返回字段约定与按 ID 查询保持一致：

- `query`
- `query_time_ms`
- `results`

### 6. 用户注册

- 路径：`POST /api/auth/register`
- 用途：注册普通用户或管理员。

请求示例：

```json
{
  "username": "demo",
  "password": "123456",
  "role": "user"
}
```

返回字段：

- `message`
- `user`

### 7. 用户登录

- 路径：`POST /api/auth/login`
- 用途：登录并返回令牌。

返回字段：

- `message`
- `token`
- `token_type`
- `login_time_ms`
- `user`

说明：

- 前端需要将 `token` 保存到本地，并在后续请求头中附带：
  `Authorization: Bearer <token>`。

### 8. 当前用户信息

- 路径：`GET /api/auth/me`
- 用途：恢复登录态、判断当前角色。

返回字段：

- `user`

### 9. 管理员用户管理

- `GET /api/admin/users`：获取用户列表
- `POST /api/admin/users`：新增用户
- `PATCH /api/admin/users/<user_id>`：更新角色或启用状态
- `DELETE /api/admin/users/<user_id>`：删除用户

前端关注的用户字段：

- `id`
- `username`
- `role`
- `is_active`
- `created_at`

## 字段稳定性约定

- `results` 必须始终返回数组，即使没有结果也返回 `[]`。
- `metadata` 建议始终返回对象，缺少字段时可为空对象 `{}`。
- 耗时字段统一使用毫秒，命名为 `*_time_ms`。
- 认证失败统一返回 401，权限不足统一返回 403，请求参数错误统一返回 400。

## 联调注意事项

- 前端默认数据路径输入为 `liver.h5ad`，若实际文件位置不同，后端需保证可正确解析。
- 过滤字段当前使用 `cell_type`、`disease`、`AgeGroup`，若成员 B 或 C 调整命名，需同步更新本清单。
- 若后端新增“数据集状态接口”或“耗时统计字段”，建议在不破坏现有字段的前提下扩展返回内容。
